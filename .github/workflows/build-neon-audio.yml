name: Build Neon Audio Mod

on:
  push:
    branches:
      - build-neon-audio-modpack-20260816
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Build iOS arm64 dylib
        run: |
          set -euxo pipefail
          SDK="$(xcrun --sdk iphoneos --show-sdk-path)"
          xcrun --sdk iphoneos clang \
            -target arm64-apple-ios15.0 \
            -isysroot "$SDK" \
            -fblocks -fno-stack-protector -fobjc-runtime=ios-15.0 \
            -dynamiclib NeonAudioModsVisible.m \
            -o NeonAudioModsVisible.dylib \
            -Wl,-install_name,@rpath/NeonAudioModsVisible.dylib \
            -framework Foundation -framework UIKit -framework AVFAudio -lobjc
          file NeonAudioModsVisible.dylib
          xcrun vtool -show-build NeonAudioModsVisible.dylib || true
      - uses: actions/upload-artifact@v4
        with:
          name: NeonAudioModsVisible-dylib
          path: NeonAudioModsVisible.dylib
          if-no-files-found: error
